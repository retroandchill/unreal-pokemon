name: Build & Test
on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
jobs:
  build:
    name: Build - Linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Setup Cache
        uses: actions/cache@v2
        with:
          path: /home/runner/.ue5
          key: ${{ runner.os }}-ue5-${{ hashFiles('*.uproject') }}
          restore-keys: |
            ${{ runner.os }}-ue5-

      - name: Clone Unreal Engine Repository
        uses: actions/checkout@v2
        with:
          repository: EpicGames/UnrealEngine.git
          ref: 5.3.2-release
          path: .ue5
          token: ${{ secrets.UNREAL_ENGINE_PAT }}
          
      - name: Build Game
        run: .ue5/Engine/Build/BatchFiles/RunUAT.sh BuildEditor -project=UnrealPokemon
